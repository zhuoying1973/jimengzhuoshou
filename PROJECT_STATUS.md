# 即梦AI解析助手项目进度固化与待办手册

本手册用于记录“即梦AI提示词多功能抓取与无水印下载助手 (Web本地版)”的最新状态。本文件独立存放于项目根目录下，作为未来开发、升级的核心指引。

---

## 📅 当前进度详细总结

### 1. 核心后端服务 (`JimengHelper/app.py`)
后端基于 Python Flask 框架，在先前版本的基础上完成了以下核心优化：
*   **免登录作者主页首屏 20 部作品同步 (`/api/author/sync`)【先前迭代】**：
    *   通过裸 Python 网络发包（不携带 Cookie，零账号风控）加载他人公开主页，使用正则表达式过滤出首屏所有的作品 ID。
    *   在后台调用完全免风控详情接口 `get_item_info` 批量抓取每个作品的原画视频、喜欢数、跟拍数和原版描述。
    *   将数据去重并追加沉淀进本地数据库 `parsed_authors_cache.json`。
*   **已收录作者名录列表返回接口 (`/api/authors/list`)【先前迭代】**：
    *   读取缓存，快速返回已收录的每个作者的 sec_uid、名字、头像以及作品部数。
*   **外部书签大包一键导入合并 (`/api/author/import`)【先前迭代】**：
    *   接收浏览器一键同步书签发回的数据，安全地对作者名下的 works 进行去重、更新和合并保存。

### 2. 前端网页界面 (`JimengHelper/templates/index.html`)
前端使用 Vue 3 框架，完成了多项有关名录交互及快捷同步的重构：
*   **磨砂玻璃【已收录创作者】横向名录栏【先前迭代】**：
    *   在主界面上方新增了一个横向滚动的微卡片列表。展示每个已存作者的圆形头像（带有外围霓虹粉/劳呼吸灯效）及收录作品部数。
    *   点击任意作者微卡片，即可瞬间在下方渲染网格并播放其最新作品，完全免去了每次需要先粘贴解析链接的繁琐步骤。
*   **【一键在线同步最新】霓虹按钮【先前迭代】**：
    *   在作品网格标题右侧新增此功能。点击即触发后端的免登录首屏抓取，自动将最新作品沉淀刷新到列表。
*   **【一键全部同步书签】功能与引导【先前迭代】**：
    *   在设置面板中，配备了一段 JS 书签代码与详细的操作说明。

---

### 3. 功能锁定与极致纯净版改造 (更新于 2026-05-28)
为了实现“绿色、免密、零报错便携版，且随时可拷贝至任何电脑直接运行”的目标，我们在本轮迭代中对项目进行了彻底大清洗和重构，锁定了核心业务：
*   **后端 Cookie 提取与同步接口彻底清洗 (`JimengHelper/app.py`)**：
    *   彻底移除了带有解密算法的本地 Chrome/Edge 浏览器 Cookie 读取机制，避免了在缺失密码学依赖库（如 `cryptography`）时的报错。
    *   删除了易受风控且在未登录状态下不可用的网页抓取接口 `/api/author/sync` 和书签导入接口 `/api/author/import`，清空了相关死锁隐患。
*   **前端 UI 界面净化 (`JimengHelper/templates/index.html`)**：
    *   在设置面板中彻底清除了 Cookie 自动导入输入框及书签代码使用指南。
    *   移除了作品网格右上角的“在线同步最新”按钮，避免误触触发验证。
    *   清理了 Vue 实例中废弃的响应式变量（如 `syncing`、`bookmarkletCode`），前端代码恢复极致简洁。
*   **本地数据库去污净化 (`parsed_authors_cache.json`)**：
    *   清理了本地模拟数据库中纽扣名下因旧版本 Bug 误吞的 5 个非纽扣本人的广场脏数据作品。
    *   恢复了纽扣本人在官网上真实的 2 个代表性作品（《贵妃怒碎瓷杯》、《高空跳伞畅玩》）的原版长提示词和播放流，库结构归于纯净。
*   **Git 初始化与 GitHub 仓库推送**：
    *   自动生成了 `.gitignore`，安全过滤了 `python-embed/` 绿色运行环境和 `scratch/` 开发草稿目录。
    *   将代码仓库与您的 GitHub 新建项目 `https://github.com/zhuoying1973/jimengzhuoshou.git` 成功绑定，配置本地 Git 用户并完成了主分支的首次安全推送。

---

## 📝 待办事项与后续开发计划

### 1. 便携式打包与单文件封装
*   **目标**：将 `JimengHelper` 制作成 Windows 平台双击即可运行的独立 `.exe` 可执行文件。
*   **说明**：虽然打包成 `.exe` 很方便，但也需要支持通过拷贝整个 `JimengHelper` 文件夹到其他电脑直接点击运行的“绿色拷贝”能力。

### 2. 双端或多端布局体验微调
*   **目标**：测试在平板端或大屏幕手机上，网页的瀑布流是否对齐，确保创作者名录栏的横向滚动效果顺滑。

---

## 🚀 2.0 创意提示词工坊开发总结 (更新于 2026-05-28)
在锁定 1.0 绿色纯净版后，我们今天正式启动并完成了 2.0 版本【创意提示词工坊】的核心功能建设：
*   **后端 API 铺设与导演预设开发 (`JimengHelper/app.py`)**：
    *   新增了 POST 接口 `/api/prompt/generate`。
    *   配置了专用的 `DIRECTOR_SYSTEM_PROMPT` 导演大模型指令，指挥阿里通义千问模型以极高标准扩写 15秒电影级高品质提示词，限制在 600 字内以防阉割。
    *   实现双版本拆分机制，同时返回分镜时间轴控制版（Version A）和长句叙事电影级融合版（Version B）。
*   **前端毛玻璃工坊面板交互 (`JimengHelper/templates/index.html`)**：
    *   在网页解析框正下方，新增了独立的【2.0 创意提示词工坊】暗黑毛玻璃板块。
    *   配备了肖哥专属的画质风格、镜头运镜“快捷单选发光标签组”，以及“阿福帮我专业润色”霓虹按钮。
    *   完成了 Vue 3 变量双向绑定与异步网络联调方法 `generatePrompt()`，并支持生成结果一键快速复制代码。
*   **开发验证与单元测试 (`scratch/test_prompt_generator.py`)**：
    *   编写了专用的虚拟客户端接口自检脚本，彻底跑通了异常参数拦截逻辑，测试 100% 通过。

## 🚀 2.0 声音与五大专业分类深度重构 (更新于 2026-05-28)
肖哥提出“视频是音画一体的，声音至关重要”，为此阿福迅速行动，展开了针对 2.0 创意工坊的深度重构与代码修复：
*   **后端 API 接口清理与音画同步导演指令重构 (`JimengHelper/app.py`)**：
    *   彻底消除了此前编辑冲突遗留的语法报错，精简重构了 `/api/prompt/generate` 路由。
    *   重新设计并升级了 `DIRECTOR_SYSTEM_PROMPT` 导演提示词，向大模型注入了高水准的“视听一体化”扩写规则：
        *   **Version A (分镜时间轴版)**：硬性规定必须在 00:00-00:05，00:05-00:10，00:10-00:15 的三阶段中，融入 BGM 节奏切入、人声旁白/独白进入的句式时机，以及配合画面高潮在 00:10-00:15 瞬间发生的重鼓点或物理声效爆破卡点。
        *   **Version B (长句融合版)**：将画面、运镜、光影、细节与声音背景音乐、沧桑旁白质感描述、特定物理音效卡点完全糅合，且自然夹带英文专业术语，总字数严格控制在 600 字内以防即梦后台提炼阉割。
*   **前端 5 大专业分类点选面板上线 (`JimengHelper/templates/index.html`)**：
    *   将原本简易的两排分类扩展为 SD WebUI 级别的 **5 大专业点选维度**：镜头运镜、光影照明、画质风格、细节画质、声音与配乐。
    *   设计了丰富的霓虹呼吸灯标签，其中“声音与配乐”提供多种人声旁白与 BGM 物理碰撞音效卡点的精美组合供一键点选。
    *   Vue 3 的 `setup()` 中挂载了三个新分类的响应式绑定与 Tags 数组，并在生成时打包传递 5 类参数发送给后台。
*   **接口测试与端到端跑通 (`scratch/test_prompt_generator.py`)**：
    *   更新测试脚本，覆盖 5 个参数的完整 payload 数据，虚拟测试客户端 100% 校验拦截成功，无任何 Python 语法和编码报错。

## 🖼️ 3.0 本地图片智能反推提示词延伸 (更新于 2026-05-30)
在原有即梦分享链接反推封面提示词的基础上，我们今天实现了“本地图片一键反推分析提示词”的延伸卡片：
*   **后端 API 对接与直传短路重构 (`JimengHelper/app.py`)**：
    *   升级了 `/api/reverse` 接口，使之不仅支持 `image_url`，同时支持 `image_base64` 参数。
    *   当检测到前端传来本地图片的 Base64 数据流时，跳过网络图片下载逻辑，直接传入阿里通义千问多模态模型 `qwen-vl-max` 中反推，实现了高效的免代理传输，性能更优。
*   **前端高颜值【本地图片反推工坊】上线 (`JimengHelper/templates/index.html`)**：
    *   在链接提取卡片下方开辟了全新板块。设计了虚线霓虹呼吸灯的拖拽和点击上传区域 `.upload-zone`，极具暗黑科技质感。
    *   结合 Vue 3 绑定与 FileReader 逻辑，实现本地拖拽/选择文件后，实时进行图片数据 Base64 读取并在前端呈现带有删除按钮的毛玻璃预览画面。
    *   支持“方案一：十维结构拆解”和“方案二：叙事长句融合”两种反推分析模式。
*   **打通功能大闭环，实现一键联动再润色**：
    *   在反推分析结果下方增加了“✨ 送入创意工坊再润色”联动按钮。
    *   点击可一键将提取出来的提示词直接填充进“2.0 创意提示词工坊”的创意输入框内，并以平滑动效（smooth scroll）自动将视图滑动到工坊面板，免去用户手动复制的繁琐，打通了“提取/反推 -> 自动带入润色 -> 生成终极短视频词”的完美操作闭环。
*   **自测验证成功 (`scratch/test_image_reverse.py`)**：
    *   编写了专用的图片反推接口自检测试，模拟 Base64 请求，拦截与直传调用测试顺利，无任何控制台编码报错。
## ⭐ 3.5 创作者名录恢复、优秀词收集夹与 UI 布局重构 (更新于 2026-05-31)
为满足肖哥对操作便利性以及日常学习他人优秀提示词的需求，本轮迭代对整个项目的界面布局和状态保存机制进行了深度优化：
*   **后端名录补齐与本地独立收藏夹接口开发 (`JimengHelper/app.py`)**：
    *   重新补齐了先前大清洗中被误删的创作者名录获取路由 `GET /api/authors/list`，读取已收录创作者并计算其收录作品数量，确保顶部名录卡片能够正常加载。
    *   新增了三个本地收藏夹操作接口：读取列表 `GET /api/favorite/list`、一键添加 `POST /api/favorite/add`、取消收藏 `POST /api/favorite/remove`。
    *   采用数据隔离设计，收藏的数据独立存放在 `scratch/favorites_cache.json` 中，有效防止不同作者的数据串扰污染。
*   **前端 HTML 核心大移位与收藏夹交互设计 (`JimengHelper/templates/index.html`)**：
    *   **核心布局重组**：将【解析结果卡片】及【创作者公开作品网格卡片】物理上移至【链接输入卡片】的正下方第一位置，将【本地图片反推】和【创意工坊】移至中下方，免去了每次解析新作品都要大滚屏至最下方的痛苦。
    *   **高亮“优秀词收集夹”卡片**：在顶部已收录创作者横向滚动条的首位，常驻设计了一个亮粉色星星的“优秀词收集夹”高发光卡片。点击可平滑切换为收藏夹视图。
    *   **一键星标收藏**：在解析卡片的功能区提供了具有 neon pink 炫光效果的“⭐ 收集提示词”/“已收集该词”按钮，点击即可实时写入本地；在作品缩略图卡片上设计了“⭐ 已收集”闪烁角标。
    *   **Vue 3 计算属性重构**：升级 `filteredWorks` 计算属性，支持根据 `isShowingFavorites` 的状态自适应输出特定作者的公开集或本地已收录的收藏词库，切换极为流畅。
*   **清理冲突与回归测试 (`scratch/test_favorites.py`)**：
    *   物理删除了由于开发中断而在 `templates/index.html` 尾部堆积的 HTML/JS 残余代码，修复了 `generatePrompt` 下载逻辑被改坏的隐患，保证代码 100% 格式纯净。
    *   编写了 `scratch/test_favorites.py` 自动化接口测试脚本，消除了 GBK 终端下的 Unicode Emojis 打印报错，本地虚拟测试 client 运行 100% 成功。
*   **3.5.1 紧急白屏修复与体验大升级**（紧急追加于 2026-05-31）：
    *   **Vue setup() 引用修复**：解决了由于 setup 函数中缺失 `saveSettings` 与 `downloadVideo` 函数声明而导致在 return 时抛出 `ReferenceError` 的致命 Bug，Vue 实例能够顺利挂载。
    *   **空指针可选链保护**：在 HTML 中对顶部的头像及作者名绑定增加了 Vue 3 可选链操作符 `?.` 防护（如 `result?.avatar`），彻底避免在 `result` 初始化为 `null` 时加载网页崩溃白屏的问题。
    *   **作者名录折行垂直滚动**：把已收录名录从单行横滑重构为多行折行排列（`flex-wrap: wrap`），并为其升级为了与其他卡片齐平对齐的 `.glass-card` 毛玻璃容器，限高 `155px`（约 3 排）开启 `4px` 垂直滚动条，解决了收录作者变多时的横向滑动烦恼。
    *   **头像右上角霓虹计数角标**：为收录作者头像和优秀词收集夹的星星图标，设计了绝对定位的数字计数气泡角标（分别以霓虹蓝和霓虹粉渐变显示），并移除了右侧原本大段累赘的“收录 X 部作品”文本，卡片极简胶囊化，横向空间利用率翻倍。
    *   **全局数据看板**：在创作者名录标题栏的最右侧，利用 Flex 布局设计了 margin-left: auto 对齐的全局看板，高亮展示“共计 X 个作者 X 个作品”。
    *   **名录多维动态排序切换**：在标题栏中增设微型暗黑玻璃下拉菜单，绑定 `authorSortKey` 并在前端配合 `sortedAuthorsList` 计算属性。支持“按收录先后”（默认，保留数据库自然读出序列）、“按字母 A-Z”（拼音首字母升序）、“作品数：多到少”、“作品数：少到多”的响应式动态重排。
    *   **原生提示词有无三态筛选**：将原本单调的复选框升级为了精致半透明的下拉框，支持“全部作品”、“有原生词”和“无原生词”的三态快速过滤，方便用户更有针对性地锁定有提示词的作品进行学习，或筛出无词作品进行 AI 画面反向重构。
    *   **GBK 控制台 Emoji 编码清洗**：将自检脚本中的所有 Emojis 字符清理为普通中文字符，彻底排除了 Windows 命令行下运行语法自检时发生 `UnicodeEncodeError` 报错。

---

## 🌐 4.0 阿里云服务器部署与24小时守护进程交付 (更新于 2026-06-02)
为了摆脱电脑开机限制，系统成功部署至阿里云北京机房公网：
*   **云端环境一键配建**：编写 `deploy_to_aliyun.py` 自动化部署脚本，在服务器的 Ubuntu 22.04 镜像上自动装配 Python3、venv 虚拟环境并配置 Flask 等全部核心依赖。
*   **Systemd 守护运行 (jimeng.service)**：在 Linux 中注册 Systemd 系统守护服务，配置服务开机自动启动，且在进程崩溃时秒级拉起，保障 24 小时稳定运行。
*   **安全组配置与公网直连**：在阿里云安全组后台放行自定义 TCP 协议的 5000 端口，手机端和电脑端通过公网直接访问：`http://8.141.3.238:5000`。

## 🔄 4.5 本地-云端数据双向一键同步 (更新于 2026-06-02)
为了方便在本地电脑端与云端手机版之间自由传输和合并数据：
*   **双向同步核心脚本**：编写并固化 `sync_from_aliyun.py`（拉取云端到本地）与 `push_to_aliyun.py`（本地上传到云端）脚本，内置自动无损数据备份（保存为 bak.json）防灾机制。
*   **Windows 一键批处理套件**：在根目录下创建了两个一键执行的批处理文件，内置 chcp 65001 编码，完全解决控制台字符乱码，上传后自动发 SSH 重启指令重载云端服务，无需手动管理。

## 🛠️ 5.0 提示词引擎重构与默认播放音量优化 (更新于 2026-06-05)
对用户界面声音感知和“AI 导演”多模态提示词格式进行了专业级精细重塑：
*   **视频播放默认静音**：在 `<video>` 组件中添加 muted 属性，使作品自动播放时保持静音，支持用户手动调节声音。
*   **失效直链静默自动续签 (核心修复)**：在播放器上挂载 @error 异常监听，视频链接因时效过期时，后台静默自动调用解析路由获取最新直链，并刷新前端显示、自动写回本地 parsed_authors_cache.json 缓存中，实现无感自愈。
*   **导演级提示词格式重构**：将工坊大模型的系统提示词中 Version A 的输出格式重构为标准的头部元数据（时长、风格、配乐）+ 表格头三横线隔离 (`---`) + 四要素 Markdown 分镜表格 + 结尾收官情绪链，强制禁用了所有 Emoji 表情符，规避了日志和老旧终端的字符乱码问题。

## 📋 6.0 剪贴板自动侦测解析 (更新于 2026-06-05)
实装了一键无感链接提取收录交互：
*   **系统设置面板配置**：前端设置卡片中增加“剪贴板自动侦测解析”配置开关，绑定 localStorage 状态记忆。
*   **焦点事件侦测与历史防抖**：全局监听网页 visibilitychange、focus 与 click 事件，在 HTTPS 安全环境下自动捕获系统剪贴板中的即梦分享链接进行无感自动解析，并设置历史比对防刷机制，保障灵敏与安全。

## 🎬 7.0 视频无水印智能抽帧与双锚点分镜反推 (更新于 2026-06-05)
*   **OpenCV 镜头突变相关性扫描**：后端自动代理中转下载原片，使用 OpenCV HSV 联合直方图算法对前后帧进行色彩比较，灵敏标记镜头突变点帧 N。
*   **黄金双锚点公式实装**：按分镜设计，抽取第 1 帧、切换点 N 前后的 N-1 帧与 N 帧、以及最后一帧，实现 8 帧上限内的分镜时序细节完美锁定。
*   **多图 VL 大模型重塑**：组装 Base64 关键帧图片列表送入 qwen-vl-max，结合时序指南生成导演级 12秒分镜时序剧本，支持无视频自动优雅降级为单图反推模式。

## 🎵 v1.2.0 音频真实听音分析与商业版本管理 (更新于 2026-06-05)
*   **FFmpeg 静默音频剥离**：后台 0.1 秒内快速将原视频剥离为 16kHz 单声道 MP3 临时文件，物理安全销毁。
*   **千问语音大模型集成**：调用 qwen-audio-turbo 语音大模型分析 BGM、乐器、物理音效特征以及人声旁白（字数放宽至 120字），融合注入 qwen-vl-max 完成音画完美卡点。
*   **商业版特权硬切割阀门**：在代码头部为抽帧模式和听音模式预留了基于 user_level 权限认证的 VIP/SVIP 拦截门槛，可一键实现商业化分级限制。

## 🐛 v1.2.3 解决多模态大模型 400 校验报错 (更新于 2026-06-06)
*   **对齐多模态原生端点**：将 qwen-vl-max 与 qwen-audio-turbo 的大模型调用 Endpoint 修正为阿里云百炼的多模态专用原生接口（`multimodal-generation/generation`），完全解决因向文本端点发送多模态入参导致的 url error 报错。
*   **重构入参格式与字符净化**：将 payload 中兼容 OpenAI 的格式重构回 DashScope 原生 `{"image": ...}`、`{"text": ...}` 与 `{"audio": ...}` 结构，并清洗了本地 app.py 残留的拉丁文乱码，消除反推提示词中的问号乱码，成功热部署于云端服务器。

---

## 🚀 8.0 作品多维打标签与双向累加同步 (更新于 2026-06-07)

为了实现给作品自由打上多维标签、按自定义分类进行检索，并且严格限制界面交互变化范围，我们今天完成了 8.0 标签系统与累加同步系统的升级开发：

*   **后端打标签接口设计与同步落盘 (`JimengHelper/app.py`)**：
    *   新增 `POST /api/work/tags/update` 接口，接收作品 ID `work_id`、作者唯一标识 `sec_uid` 以及标签列表 `tags`，自动对传入的标签数组执行剔除空字符、去除空格及去重排序清洗。
    *   打标签操作会同步覆盖更新“创作者作品库（`parsed_authors_cache.json`）”与“优秀词收集夹（`favorites_cache.json`）”中对应作品的 `tags`，保持两端标签一致。
    *   在解析与获取作者作品、收藏列表等关键接口中，全面加装 `setdefault("tags", [])` 初始化防空兜底，保障历史老旧数据不报错。
*   **前端磨砂霓虹标签云与局部内聚悬浮编辑 Popover (`JimengHelper/templates/index.html`)**：
    *   **动态去重标签云**：在顶部创作者名录下方新增 `.tags-cloud-panel` 容器，利用 Vue 3 计算属性 `allTags` 动态提取并去重当前列表包含的所有标签，以彩色 HSL 渐变胶囊卡片渲染，点击即可前端零延迟过滤筛选。
    *   **严格控制变化在卡片内**：摒弃全局模态弹窗，设计了绝对定位的 `.tag-editor-popover` 与 `.result-tag-editor-popover` 局部悬浮气泡，完全局限在触发卡片（缩略图网格和详情区）范围内展示。
    *   **常用标签推荐**：利用 Vue 计算属性 `historyTags` 自动提取历史已有的高频标签，支持手机端一键点选输入，并支持回车录入和一键删除。
*   **完全去除 Emoji（消除 GBK 乱码隐患）**：
    *   将 `templates/index.html` 中可能在 Windows GBK 控制台和老旧终端下产生 `UnicodeEncodeError` 的所有 Emojis 表情符彻底清洗。
    *   重构为更高端、具有溢价感的 **FontAwesome 霓虹图标**（如 `fa-tags` 霓虹蓝图标、`fa-clapperboard` 霓虹粉图标、`fa-video` 霓虹蓝图标），既消除了乱码隐患又提升了 UI 美感。
*   **双向数据无损累加合并同步重构 (`JimengHelper/scratch/`)**：
    *   彻底将原本粗暴的文件强覆盖式同步（`sync_from_aliyun.py` 与 `push_to_aliyun.py`）重构为**智能双向累加合并（Merge）机制**。
    *   当运行同步时，系统在后台自动“下载云端缓存 -> 在内存中与本地缓存进行创作者和作品 id 的并集去重合并 -> 标签 tags 合并 -> 同步写回两端 -> 重启云服务”。
    *   从根本上消除了因“操作顺序点反”或“多端单独解析”导致的数据覆盖冲刷，无论点“上传”还是“下载”均能无损合出最新最全汇总。
    *   在每次合并同步前，仍然在本地自动进行旧数据库的 bak 备份，确保历史数据防灾。
